# P2-1-2 행성별 Planet Content 검증

## 현재 판정

- 상태: 최신 `origin/dev` (`e422255`) Snapshot schema v3 authoritative `mapId` 가드 및 local/dev 자동 재검증 완료, kinggusi 사람 비주얼 PASS·jjangash 승인 대기
- 자동 테스트: PlanetContent·Session Adapter·State Authority·reconnect 집중 72/72, Unity 전체 EditMode 504/504 PASS
- Unity 에셋 생성: 9개 행성 placeholder 생성 및 canonical validator PASS
- 사람 비주얼 검증: kinggusi PASS, jjangash 대기
- production 완료 게이트: P1-5-7 JWT/matchmaking Adapter 및 실제 production 경계 2클라이언트 Smoke Test

## Snapshot schema v3 후속 검증 (2026-09-02)

- 기준: `origin/dev` `e422255076d70616b7fcbb0676d0abaa25e47d55` (PR #110 병합)
- Shared schema v3가 추가한 `BattleSessionSnapshot.mapId`를 Battle 캡처 경계에서 다시 검증한다.
- spawned State Authority의 `AuthoritativeMapId`가 누락되거나 `BattleSessionContext.MapId`와 ordinal exact match가 아니면 Snapshot 생성을 fail-closed한다.
- Snapshot에는 로컬 Session 문자열이 아니라 검증을 마친 authoritative Fusion 값을 투영한다.
- `SessionSource`는 서버가 trusted roster 경로에서 확정하는 값이므로 Unity Snapshot에 추가하거나 클라이언트가 전송하지 않는다.
- 신규 mapId 성공·누락·불일치·대소문자·공백 케이스를 포함한 집중 EditMode 72/72 PASS.
- Unity 전체 EditMode 504/504 PASS, failed 0, skipped 0, compile error 0.
- 증거: `C:/myDefense/_localbuild/P2Validation/p2-1-snapshot-mapid-targeted.xml`, `C:/myDefense/_localbuild/P2Validation/p2-1-snapshot-mapid-full.xml`.
- 이 Thread에서 호출 가능한 Unity MCP 도구는 0개였다. Scene/Prefab 변경은 없으며 기존 자동 Scene 검증을 전체 EditMode 회귀에 포함했다.

## 최신 dev 동기화 (2026-08-31)

- 기준 feature HEAD: `6667e827c3b68b1adc7367a8ca7e363ecd468fe1`
- 병합 대상: `origin/dev` `3816faec478cbf502fe2ff3361a9526c6cee53ec`
- 병합 방식: `git merge --no-commit --no-ff origin/dev` (commit/push 없음)
- 충돌 해결: `BattleWaveStateAuthorityTests`에서 authoritative `mapId` binding과 최신 cooperative kill Gold 계약을 모두 보존했다.
- P2-1-1 호환: 완료된 local/dev 행성 입장·trusted roster가 사용하는 canonical `mapId`를 `BattleSessionContext` → Fusion State Authority → PlanetContent 적용 경계에서 변경하지 않는다.
- 자동 병합 보존: 최신 Settlement·cooperative Gold·Breeding 및 서버 변경을 그대로 유지했고 Shared/Spring/User-System 코드를 직접 수정하지 않았다.
- Unity MCP: 이 구현 Thread에는 호출 가능한 Unity MCP 도구가 없었다. Scene/Prefab은 변경하지 않고 기존 Unity Editor API 생성 에셋을 읽기·테스트·빌드로 검증했다.

## 확정 범위

- 단일 `Battle.unity` 재사용
- authoritative `BattleSessionContext.MapId`와 대소문자까지 정확히 일치하는 Profile만 적용
- State Authority가 `NetworkString<_16>` authoritative mapId를 최초 1회 고정하고 Client는 복제 완료 전까지 초기화 재시도
- 로컬 SessionContext mapId와 replicated authoritative mapId가 다르면 양쪽 모두 fail-closed
- `PlanetContentProfile` ScriptableObject에 환경 Prefab, 배경 Sprite/Color, 환경 Material, 조명, Particle/환경 Effect 참조 보관
- 환경 Prefab은 presentation-only로 제한
- Board, Lane, Waypoint, Wave Executor, Boss Runtime, Fusion `NetworkObject` 포함 금지
- 공통 Board/Lane/Waypoint/Boss 전투 구조 유지
- Additive Scene, 행성 고유 기믹, 신규 Boss 패턴, 별도 컷신 제외
- 알 수 없는 mapId와 누락·중복·비활성 Profile은 fallback 없이 Wave 시작 전에 fail-closed

## 구현 구조

| 구성 | 역할 |
|---|---|
| `PlanetContentProfile` | mapId별 환경 Prefab·배경·Material·조명·Effect 설정 |
| `PlanetContentCatalog` | Resources에서 자동 로드하는 9행성 Profile Catalog |
| `PlanetContentValidator` | canonical coverage, root marker, presentation component allowlist 검증 |
| `PlanetContentApplicator` | 환경 인스턴스 교체, 배경·조명 적용, Clear 시 Scene 전역값 복원 |
| `BattleSceneSessionAdapter` | State Authority/Wave 초기화 전 authoritative mapId 적용 게이트 |
| `PlanetContentAssetGenerator` | Unity Editor API로 placeholder Material/Prefab/Profile/Catalog 생성 |

## Editor 생성기

Unity MCP는 현재 isolated 프로젝트에서 실제 호출할 수 없어 Editor API 생성기를 사용한다.

```text
MyDefense.Battle.Editor.PlanetContentAssetGenerator.GeneratePlaceholderContent
```

생성 예정 에셋:

- `Assets/Resources/Battle/PlanetContent/PlanetContentCatalog.asset`
- `Assets/Resources/Battle/PlanetContent/Profiles/<MAP_ID>.asset` 9개
- `Assets/Prefabs/Battle/PlanetContent/<MAP_ID>_Environment.prefab` 9개
- `Assets/Prefabs/Battle/PlanetContent/<MAP_ID>_AmbientEffect.prefab` 9개
- `Assets/Materials/Battle/PlanetContent/<MAP_ID>_Environment.mat` 9개

`.prefab`, `.asset`, `.meta`, GUID는 Unity Editor API 생성기가 직접 관리했다. 1차 실행에서 같은 파일에 선언된 `PlanetEnvironmentContent`가 Prefab Missing Script로 탐지되어 fail-fast했고, MonoBehaviour를 전용 파일로 분리한 뒤 2차 실행에서 9개 전체 생성과 canonical 검증을 통과했다.

## 자동 검증 항목

- 9개 canonical mapId exact resolve
- lowercase/unknown/null mapId 거부 및 fallback 금지
- Profile 누락·중복·비활성 전체 Catalog 거부
- production canonical PlanetBattle registry와 9개 Profile 순서·ID 대조
- 실제 AssetDatabase Resources Catalog와 Profile/Prefab/Material 참조 9세트 로드 검증
- root `PlanetEnvironmentContent` marker 필수 및 임의 MonoBehaviour를 포함한 allowlist 외 컴포넌트 거부
- State Authority mapId 최초 bind·변경 거부, Client 복제 대기·불일치 거부
- 동일 mapId 재적용 idempotent
- mapId 변경 시 이전 환경 제거 후 교체
- 잘못된 mapId 적용 실패 시 기존 환경 보존
- Clear 시 Camera/Directional Light/RenderSettings 복원
- Session Adapter가 잘못된 mapId를 Wave Session 초기화 전에 차단

## 실행 후 기록할 결과

| 검증 | 결과 |
|---|---|
| Unity compile errors | 0 |
| PlanetContent·Session Adapter·State Authority·reconnect 집중 EditMode | 72/72 PASS |
| Unity 전체 EditMode | 504/504 PASS |
| Catalog/Prefab validator | PASS |
| Battle Scene validate | MCP 미가용; 전체 EditMode의 Battle Scene topology/executor 검증 PASS, issue 0 |
| Windows Development Build | PASS — `Battle.unity` 단독, compile error 0 (`C:/myDefense/_localbuild/P2Validation/P2PlanetPR102Sync/Client.exe`) |
| 독립 리뷰 | PASS — authoritative mapId guard 확인, 차단 0, 문서 수치 현행화 완료 |
| kinggusi 비주얼 검증 | PASS — 9행성 placeholder 구분 및 전투 화면 가독성 승인 (2026-08-28) |
| jjangash 비주얼 검증 | 대기 |
| Host/Client 동일 mapId 환경 표시 | 9행성 로컬 2창 자동 스모크 PASS, 사람 검증 대기 |

최신 dev 동기화 검증 증거:

- Compile: `C:/myDefense/_localbuild/P2Validation/p2-planet-pr102-sync-compile.log`
- 집중 EditMode: `C:/myDefense/_localbuild/P2Validation/p2-planet-pr102-sync-targeted.xml`
- 전체 EditMode: `C:/myDefense/_localbuild/P2Validation/p2-planet-pr102-sync-full.xml`
- Windows Development Build: `C:/myDefense/_localbuild/P2Validation/p2-planet-pr102-sync-build.log`

## Windows Development Build 및 로컬 스모크

- Build: `Client/_localbuild/P2PlanetContent_20260827_R3/Client.exe`
- Build Settings: `Assets/Scenes/Battle.unity` 단독
- Build result: Success, compile error 0, 기존 obsolete/unused field warning 14
- Session: 행성별 독립 `P1VAL-<MAP>-W010-<nonce>` 세션
- Host: `dev-host`, Client: `dev-client`
- Fusion 입장: 양쪽 PASS
- Session Adapter 초기화: 양쪽 PASS
- authoritative map: 9개 canonical map 모두 mismatch/fail-fast 오류 없음
- Host/Client 입장·응답·동일 행성 환경 표시: 9/9 PASS, 관련 Console Error 0
- Particle Material은 Transparent Queue, premultiplied alpha, `ZWrite=0`을 명시하여 불투명 점 및 핑크 fallback을 방지했다.
- 9행성 Host/Client 비교 화면: `Client/_localbuild/P2PlanetContent_20260827_R3/planet-9-pair-contact-sheet-r3.png`
- 최초 Development Console의 Attack Snapshot HTTP 500은 8082 Spring 로컬 서버가 실행 중인 상태에서 인메모리 H2의 `USERS` 테이블이 유실되어 발생했다. 특정 playerId, Fusion 또는 PlanetContent 회귀가 아니었다.
- 8082 서버를 local profile로 재시작해 H2 schema를 복구한 뒤 `dev-host`, `dev-client` Snapshot API가 각각 HTTP 200과 Alien 48개를 반환하는 것을 확인했다.
- 복구 후 9행성 모두 새 Host/Client 세션으로 다시 검증했으며 입장·응답 9/9, PlanetContent 관련 오류 0, Attack Snapshot HTTP 500 합계 0을 확인했다.
- 위 자동 스모크는 사람 비주얼 승인을 대체하지 않는다.

## 사람 검증 체크리스트

1. NEPTUNE부터 SUN까지 각 canonical mapId로 새 2인 Session을 시작한다.
2. Host/Client가 같은 행성 배경색·환경 장식·조명·Particle을 표시하는지 확인한다.
3. 두 화면의 Board, 개인 Lane, Waypoint, 공용 Boss Lane 위치가 기존과 동일한지 확인한다.
4. 10 Wave Boss 생성·이동·소멸과 일반 Monster 경로가 행성 환경 적용 후에도 동일한지 확인한다.
5. unknown/lowercase/비활성 mapId 검증 Build에서는 Wave가 시작되지 않고 오류가 한 번만 표시되는지 확인한다.
6. Session 종료·재진입에서 이전 행성 환경이 남지 않는지 확인한다.
7. Console Error, Missing Script/Reference, NetworkObject 중복 등록이 없는지 확인한다.

## 현재 위험

- placeholder는 최종 Asset이 아니므로 행성별 최종 가독성과 연출 품질은 비주얼 검증 대기다.
- production mapId 권위는 P1-5-7 Adapter 제공 전까지 local/dev 계약만 검증할 수 있다.
- Shared `BattleSessionSnapshot` schema v3의 `mapId` 계약과 Battle 캡처 경계 가드는 반영됐다. 실제 Snapshot 복원 transport와 production 인증 Adapter가 아직 없으므로 production 재접속 E2E는 별도 게이트로 유지한다.
- Camera/조명과 환경 Prefab 배치는 실제 `Battle.unity` 기준 해상도·카메라에서 사람 검증이 필요하다.
- 현재 Resources Catalog는 9개 행성 Prefab/Material을 직접 참조하고 초기화 때 전체 hierarchy를 동기 검증한다. 약 1.2MB placeholder 범위에서는 비차단이지만 최종 고용량 Asset 교체 시 Addressables 또는 검증 결과 캐시를 검토한다.
