# P1-5-6/7 Settlement E2E · Trusted Roster 검증

## 범위

- Unity Fusion 참가자 두 명을 Spring trusted roster에 등록하는 local/dev Adapter
- roster 등록 성공 전 Wave/Settlement 진행 차단
- Unity `BattleSettlementSummary`와 Spring `BattleSettlementDtos` 1:1 계약
- Wave 80 종료 Summary 저장, 영구 보상 지급, 동일 요청 멱등 재처리
- production JWT/matchmaking Adapter 교체 경계

## 인증 경계

- 현재: `BattleSessionRosterRegistrar` → loopback `/api/dev/battle/session-rosters`
- 적용 환경: Spring `local`/`dev`, Unity `local`/`dev`
- 로컬 E2E 사용자: 명시적인 `dev-*` ID만 local DB 자동 생성
- 운영: `FUTURE_AUTH_REPLACEMENT` 표식의 구현을 JWT principal + matchmaking 검증 Adapter로 교체
- 운영 Adapter 부재 시 fail-closed
- Spring Profile 생략/prod에서는 개발 Controller와 Adapter bean이 생성되지 않음
- 두 참가자는 Session 단위 단일 원자 연산으로 등록되어 경쟁 요청의 slot 혼합을 방지
- 일시 실패는 `RetryRosterRegistration()` 명시적 진입점으로만 재시도

## 자동 검증 결과

- Spring targeted roster + Settlement E2E: 통과
- Spring 전체: 310/310, failed 0, skipped 0
- BalanceTool: 77/77, failed 0, skipped 0
- Unity EditMode: 378/378, failed 0, skipped 0
- Battle Scene validate: issue 0, Missing Script 0, Broken Prefab 0
- `git diff --check`: 통과

## E2E 보상 검증

- trusted roster: 서로 다른 두 사용자, slot 1/2
- 결과: NEPTUNE, VICTORY, Wave 80, status ACCEPTED
- 사용자별 지급: Gold 25,250 / Universal Piece 225 / Diamond 3,000
- 첫 응답: `alreadyProcessed=false`
- 동일 requestId/sessionId/summaryHash 재요청: `alreadyProcessed=true`
- 재요청 후 사용자 잔액 불변

## 실제 Fusion Host/Client 종료 검증 — 2026-08-27

- 기준: `dev` merge commit `e6ba317`
- Session: `P1SET-20260827-1907-e6ba317f00d2` (새 non-P1VAL)
- Host: `dev-host`, slot 1
- Client: `dev-client`, slot 2
- Map: `NEPTUNE`
- `[BattleRoster] registered trusted local roster`가 W001 시작보다 먼저 정확히 1회 출력됨
- W001~W009 실제 처치 후 W010 Boss 30초 timeout으로 `DEFEAT` 생성
- 실제 Summary: `finalWave=9`, Normal Kill 256, Boss Kill 0, Kill Gold 5,120
- Host: Kill 121, Support 23, in-game Gold `100000 + 2560 - 110 = 102450`
- Client: Kill 135, Support 16, in-game Gold `100000 + 2560 - 110 = 102450`
- requestId: `7760141d9f884e9596c57e8f1e80c150`
- summaryHash: `1d107a792dcd8e5bcc8084d3308721d8006a4648f3af7b4b87909e30c4d81c91`
- 최초 응답: `ACCEPTED`, `alreadyProcessed=false`, rewards 없음
- 캡처한 동일 JSON payload 재전송: `ACCEPTED`, `alreadyProcessed=true`
- H2: Settlement 1건, Player 2건, 금액 0의 `SETTLEMENT` Claim 2건
- Retry 전후 `dev-host`·`dev-client`의 Gold·Universal Piece·Diamond 불변
- 기존 사전 Summary와 P1VAL Summary POST 없음

## 실플레이 발견 결함과 조치

- 발견: W010 timeout 뒤 Match와 Settlement는 종료됐지만 Boss NetworkObject가 양 화면에 남아 계속 이동함
- 원인: `TryResolveBossTimeout()`이 `FAILED` 전환 후 현재 Boss를 Release하지 않음
- 조치: terminal 전환 성공 직후 State Authority가 `ReleaseCurrentBoss()`로 권위 Despawn
- 보존: timeout은 Monster 사망·Boss 처치·Kill audit·Kill Gold·보상 경로를 호출하지 않음
- 자동 검증: 관련 EditMode 36/36, 전체 EditMode 439/439, 컴파일 오류 0
- 실제 스모크: `P1VAL-NEPTUNE-W010-f00de6ba317c`에서 Host/Client 양쪽 Boss 소멸 PASS
- 독립 리뷰: Blocker 0, Major 0, 최종 PASS

## 판정

- P1-5-6 local/dev 실제 Settlement E2E: PASS, 완료
- P1-5-7 local/dev Adapter: PASS
- production JWT/matchmaking Adapter: 별도 인증 기반 작업으로 유지
